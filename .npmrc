# Evita que Puppeteer descargue Chromium en `pnpm install`.
# En desarrollo, configura `CHROMIUM_PATH` en .env apuntando a tu Chrome/Edge.
# En produccion el Dockerfile instala chromium via apk y setea las envs.
puppeteer-skip-download=true
